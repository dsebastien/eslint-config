# AGENTS.md

## Scope

- This file applies to `src/template/`.

## What Lives Here

- Angular template lint configuration.

## Working Rules

- Keep template-specific rules separate from TypeScript/source rules.
- When changing parser or Angular template plugin behavior, validate against a real Angular consumer such as `stark`, `showcase`, or `starter`.
